# Static analysis for Sticky Wall. Run with `flutter analyze`; CI and the
# release checklist require a clean result.
include: package:flutter_lints/flutter.yaml

analyzer:
  # Generated localizations are not hand-maintained; don't lint them.
  exclude:
    - lib/l10n/app_localizations*.dart
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    # Consistency
    prefer_single_quotes: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    unnecessary_lambdas: true
    unnecessary_parenthesis: true
    directives_ordering: true
    sort_pub_dependencies: false
    # Correctness
    avoid_dynamic_calls: true
    avoid_print: true
    cancel_subscriptions: true
    close_sinks: true
    unawaited_futures: true
    use_build_context_synchronously: true
    # Documentation for public API in lib/
    public_member_api_docs: false
